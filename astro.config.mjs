// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://zzy7675.github.io',
//   base: '/<repo-name>', // omit only if the repo is <username>.github.io
});
